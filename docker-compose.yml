version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: devtools-hub-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    restart: unless-stopped
    environment:
      - MONGO_INITDB_DATABASE=devtools-hub

volumes:
  mongo-data:
